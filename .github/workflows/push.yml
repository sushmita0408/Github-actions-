name: Basic Push Workflow

on: 
  push:
  workflow_dispatch:

jobs:
  basic-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "✅ This workflow ran because code was pushed to the main branch."
